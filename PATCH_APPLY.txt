MISHARP DAILY REPORT v3.4.1 — Cafe24 회원가입 수집 보강 통합본

v3.4.0 전체 수정사항을 그대로 포함한 누적 패치입니다.
이 ZIP 하나만 덮어쓰면 됩니다.

이번 수정:
1. Cafe24 Dashboard 응답이 dict/list/nested 형태여도 new_members_count를 재귀 탐색
2. new_members_count가 0이어도 정상값 0으로 DB 저장
3. Cafe24 신규회원 API 단독 테스트 버튼 추가
4. API 오류가 발생하면 더 이상 조용히 '자료없음'으로만 끝나지 않고 화면에 실제 오류 표시
5. 오늘 Cafe24 수집 후 회원가입 DB 반영 여부를 즉시 표시
6. sync 로그에도 member_signups 값/오류 기록

Cafe24 공식 Admin Dashboard:
- GET /api/v2/admin/dashboard
- scope: mall.read_store
- property: new_members_count

덮어쓸 파일은 ZIP 안 경로 그대로 모두 적용.
DB schema / Secrets 변경 없음.
OAuth를 이미 mall.read_store 포함 상태로 다시 승인했다면 추가 재승인은 필요 없음.
